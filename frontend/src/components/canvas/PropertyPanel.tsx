/**
 * canvas/PropertyPanel.tsx
 *
 * Canonical re-export so that the canvas directory and any external
 * imports referencing this path resolve to the full implementation.
 * The actual implementation lives in components/properties/PropertyPanel.tsx.
 */
export { PropertyPanel } from "@/components/properties/PropertyPanel";
