"""Core package for Archon backend.

Provides import compatibility for ``app.core.*`` imports.
Core utilities live here; shared config/db remain at ``app.config`` and
``app.database`` (flat layout preserved for backward compatibility).
"""
